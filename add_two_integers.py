class Solution:
    def sum(self, num1: int, num2: int) -> int:
        
        total= num1+num2
        
        return total
    
    #take the 2 numbers and add them- you can also just add them without storing them as a variable